# ====================================
# FIREBASE HOSTING - AUTO DEPLOY ON MERGE
# ====================================

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Create .env.production file
        run: |
          mkdir -p config/credentials
          echo "VITE_FIREBASE_API_KEY=${{ secrets.VITE_FIREBASE_API_KEY }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_PROJECT_ID=${{ secrets.VITE_FIREBASE_PROJECT_ID }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_APP_ID=${{ secrets.VITE_FIREBASE_APP_ID }}" >> config/credentials/.env.production
          echo "VITE_FIREBASE_DATABASE_URL=${{ secrets.VITE_FIREBASE_DATABASE_URL }}" >> config/credentials/.env.production
          echo "VITE_BINANCE_API_URL=https://api.binance.com" >> config/credentials/.env.production
          echo "VITE_HYPERLIQUID_API_URL=https://api.hyperliquid.xyz" >> config/credentials/.env.production
          echo "VITE_ENABLE_DEBUG_LOGS=false" >> config/credentials/.env.production
          echo "VITE_ENVIRONMENT=production" >> config/credentials/.env.production
      
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_COOKIE1_B3592 }}
          channelId: live
          projectId: cookie1-b3592
